% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate.R
\name{vm_add_images}
\alias{vm_add_images}
\title{Add images at cell centroids}
\usage{
vm_add_images(
  p,
  vm = NULL,
  image,
  size = 0.05,
  by = "width",
  asp = 1,
  alpha = 1,
  nudge_x = 0,
  nudge_y = 0,
  cells = NULL
)
}
\arguments{
\item{p}{A ggplot from \code{\link[=autoplot.voronoi_map]{autoplot.voronoi_map()}} / \code{\link[=ggvmap]{ggvmap()}}.}

\item{vm}{Optional \code{voronoi_map}; taken from \code{p} when omitted.}

\item{image}{Image paths or URLs: length-\code{n} (cell order), named by cell
label, or length 1.  \code{NA} entries are skipped.}

\item{size}{Image size as a fraction of the plot.  Default \code{0.05}.}

\item{by}{Size dimension passed to \code{\link[ggimage:geom_image]{ggimage::geom_image()}}: \code{"width"}
(default) or \code{"height"}.}

\item{asp}{Aspect-ratio correction passed to \code{\link[ggimage:geom_image]{ggimage::geom_image()}}.
Default \code{1}.}

\item{alpha}{Image opacity.  Default \code{1}.}

\item{nudge_x, nudge_y}{Position offset in data units.  Default \code{0}.}

\item{cells}{Optional subset of cell labels to annotate.}
}
\value{
The ggplot with an image layer added.
}
\description{
Places an image (logo, icon, flag, photo) at the centroid of each cell.
Requires the \pkg{ggimage} package.
}
