% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/voronoi_map.R
\name{voronoi_map}
\alias{voronoi_map}
\title{Compute a Voronoi map}
\usage{
voronoi_map(
  weights,
  labels = NULL,
  group = NULL,
  clip = clip_square(),
  convergence_ratio = 0.01,
  max_iter = 200,
  min_weight_ratio = 0.01,
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{weights}{Numeric vector of positive weights (one per cell).}

\item{labels}{Optional character vector of cell labels.}

\item{group}{Optional grouping vector (one value per cell).  When
supplied a hierarchical layout is produced.}

\item{clip}{Clipping polygon as a 2-column matrix (x, y),
counterclockwise and open.
Defaults to the unit square.}

\item{convergence_ratio}{Stop when the total area error divided by the
polygon area falls below this ratio.
Default \code{0.01} (1\%).}

\item{max_iter}{Maximum number of iterations.  Default \code{200}.}

\item{min_weight_ratio}{Minimum allowed data weight as a fraction of
the maximum weight.  Prevents near-empty cells.
Default \code{0.01}.}

\item{seed}{Integer seed for reproducible initial positions.
\code{NULL} (default) uses a random layout.}

\item{verbose}{Print iteration progress? Default \code{FALSE}.}
}
\value{
An object of class \code{"voronoi_map"} (a list) containing:
\describe{
\item{cells}{List of 2-column polygon matrices.}
\item{sites}{Data frame with columns \code{x}, \code{y}, \code{weight},
\code{target_area}, \code{actual_area}, \code{label}, \code{data_weight}
and (when hierarchical) \code{group}.}
\item{clip}{The clipping polygon.}
\item{groups}{When hierarchical: a list with the group-level \code{cells}
and \code{sites}; otherwise \code{NULL}.}
\item{hierarchical}{Logical flag.}
\item{iterations}{Number of iterations performed (top level).}
\item{convergence}{Final area-error ratio.}
\item{converged}{Logical; did convergence_ratio threshold get reached?}
}
}
\description{
Partition a convex polygon into cells whose areas are proportional
to a set of weights, using an iteratively refined power diagram.
}
\details{
When \code{group} is supplied the layout becomes \strong{hierarchical}: the boundary
is first partitioned into one convex sub-region per group (with area
proportional to the group's total weight), and each sub-region is then
filled with its member cells.  On a circular boundary the groups are seeded
radially so that they form contiguous angular sectors -- the arrangement the
\code{\link[=vm_add_ring]{vm_add_ring()}} annotation ring is designed to wrap.
}
\examples{
# Simple example: 5 sectors
vm <- voronoi_map(
  weights = c(3, 2, 5, 1, 4),
  labels  = c("A", "B", "C", "D", "E"),
  seed    = 42
)
plot(vm)

# Hierarchical example on a circular boundary
vm_h <- voronoi_map(
  weights = c(5, 3, 2, 8, 4, 1, 6, 2),
  labels  = letters[1:8],
  group   = c("X", "X", "X", "Y", "Y", "Z", "Z", "Z"),
  clip    = clip_circle(),
  seed    = 1
)
plot(vm_h)

}
