% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate.R
\name{vm_add_labels}
\alias{vm_add_labels}
\title{Add value labels at (or near) cell centroids}
\usage{
vm_add_labels(
  p,
  vm = NULL,
  value = NULL,
  secondary = NULL,
  fmt = NULL,
  prefix = "",
  suffix = "",
  size = 2.8,
  col = "grey20",
  fontface = "plain",
  nudge_x = 0,
  nudge_y = NULL,
  cells = NULL,
  inside = TRUE,
  min_area = 0,
  autoscale = FALSE,
  family = NULL,
  wrap = NULL
)
}
\arguments{
\item{p}{A ggplot from \code{\link[=autoplot.voronoi_map]{autoplot.voronoi_map()}} / \code{\link[=ggvmap]{ggvmap()}}.}

\item{vm}{Optional \code{voronoi_map}; taken from \code{p} when omitted.}

\item{value}{Values to display: length-\code{n}, named by label, or length 1.
Defaults to each cell's \code{data_weight}.}

\item{secondary}{Optional second value shown in parentheses (e.g. a count).}

\item{fmt}{A function applied to \code{value} (and \code{secondary}) for formatting,
e.g. \code{scales::comma}.  Default: \code{\link[=format]{format()}} with \code{big.mark = ","}.}

\item{prefix, suffix}{Strings wrapped around the formatted value.}

\item{size}{Text size: a single value, a length-\code{n} vector in cell order,
or a vector named by cell label (unnamed cells keep the default \code{2.8}).
Default \code{2.8}.}

\item{col}{Text colour: a single colour (default \code{"grey20"}), a length-\code{n}
vector in cell order, or a vector named by cell label (cells not named
keep the default).}

\item{fontface}{Font face: a single value (default \code{"plain"}) applied to
all labels, or a vector named by cell label (e.g.
\code{c(Brazil = "bold")}) styling only those cells while the rest stay
\code{"plain"}.}

\item{nudge_x, nudge_y}{Offset from the centroid.  \code{nudge_y} defaults to a
small downward shift scaled to each cell so the value sits below the name
without leaving small cells.}

\item{cells}{Optional subset of cell labels to annotate.}

\item{inside}{Logical; clamp the label anchor inside its cell when the
nudge would push it out?  Default \code{TRUE}.}

\item{min_area}{Cells whose area fraction of the map is below this
threshold get no value label.  Default \code{0} (label every cell).}

\item{autoscale}{Logical; shrink label text in small cells?  Each cell's
text size becomes \code{size * pmin(1, sqrt(cell_area / median_area))},
floored at 60\% of \code{size}.  Default \code{FALSE}.}

\item{family}{Font family for the value labels, passed to the text layer.
\code{NULL} (default) uses the ggplot2 default.}

\item{wrap}{Wrap value labels longer than this many characters onto
multiple lines (word-aware, via \code{\link[=strwrap]{strwrap()}}).  Default \code{NULL}
(no wrapping).}
}
\value{
The ggplot with a value-label layer added.
}
\description{
Draws a secondary text label per cell -- typically a numeric value beneath
the category name added by \code{\link[=autoplot.voronoi_map]{autoplot.voronoi_map()}}.
}
