% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate.R
\name{vm_add_ring}
\alias{vm_add_ring}
\title{Add a colored outer annotation ring}
\usage{
vm_add_ring(
  p,
  vm = NULL,
  groups = NULL,
  colors = NULL,
  labels = TRUE,
  curved = NULL,
  palette = "Okabe-Ito",
  style = c("band", "arc"),
  width = 0.1,
  gap = 0.02,
  pad = 1,
  label_col = NULL,
  label_size = 3.2,
  label_fontface = "bold",
  border_col = "white",
  border_size = 0.4,
  family = NULL,
  linewidth = 0.5,
  linetype = "solid",
  offset = 0.06,
  values = FALSE,
  values_sep = " · "
)
}
\arguments{
\item{p}{A ggplot produced by \code{\link[=autoplot.voronoi_map]{autoplot.voronoi_map()}} or \code{\link[=ggvmap]{ggvmap()}}.}

\item{vm}{Optional \code{voronoi_map}; taken from \code{p} when omitted.}

\item{groups}{Optional character vector selecting and ordering which groups
to draw.  Defaults to all groups.}

\item{colors}{Colours for the ring segments (band fills or arc lines):
\code{NULL} (default) uses the \code{palette}, so each group matches its cells; a
single colour (e.g. \code{"#333333"}) colours every segment the same; a named
vector keyed by group (e.g. \code{c(LATAM = "#333333")} with the other groups
named too) sets each group individually; an unnamed vector is
interpolated across the groups.  With \code{style = "arc"}, the labels follow
the arc colours unless \code{label_col} is set.}

\item{labels}{Logical, or a named character vector of display labels keyed
by group.  \code{TRUE} (default) uses the group names; \code{FALSE} draws no text.}

\item{curved}{Draw labels curved along the arc using \pkg{geomtextpath}?
\code{NULL} (default) curves them when that package is installed and otherwise
uses straight tangential text; \code{TRUE}/\code{FALSE} force the choice.}

\item{palette}{Palette used when \code{colors} is \code{NULL}.  Default \code{"Okabe-Ito"}.}

\item{style}{Ring style: \code{"band"} (default) draws the filled arc segments;
\code{"arc"} draws a thin line per group with the group label sitting in a gap
broken into the arc at the segment midpoint (the classic infographic
look, e.g. "NORTH AMERICA 13\%" with a middle-dot separator).}

\item{width}{Band thickness as a fraction of the map radius (\code{style = "band"} only).  Default \code{0.10}.}

\item{gap}{Radial gap between the map and the ring, as a fraction of the
radius.  Default \code{0.02}.}

\item{pad}{Angular padding trimmed from each segment end, in degrees.
Default \code{1}.}

\item{label_col}{Ring label colour.  \code{NULL} (default) means \code{"white"} for
\code{style = "band"} and each arc's own colour for \code{style = "arc"}.}

\item{label_size}{Ring label size.  Default \code{3.2}.}

\item{label_fontface}{Ring label font face.  Default \code{"bold"}.}

\item{border_col}{Segment border colour (\code{style = "band"}).  Default \code{"white"}.}

\item{border_size}{Segment border width (\code{style = "band"}).  Default \code{0.4}.}

\item{family}{Font family for the ring labels.  \code{NULL} (default) uses the
ggplot2 default.}

\item{linewidth}{Arc line width (\code{style = "arc"}).  Default \code{0.5}.}

\item{linetype}{Arc line type (\code{style = "arc"}), e.g. \code{"dashed"}.
Default \code{"solid"}.}

\item{offset}{Distance of the arc and its label from the map edge, as a
fraction of the radius (\code{style = "arc"}; \code{width} is ignored).
Default \code{0.06}.}

\item{values}{Append each group's share to its label (\code{style = "arc"}),
e.g. "LATAM 32\%" with a middle-dot separator?  Computed from the group
weights.  Default \code{FALSE}.}

\item{values_sep}{Separator between the group name and its share.
Default is a middle dot (\code{" \\u00b7 "}).  On Windows \code{pdf()} devices the
dot can hit an encoding conversion failure -- pass an ASCII separator
such as \code{" - "} there.}
}
\value{
The ggplot with ring layers added.
}
\description{
Wraps a circular Voronoi map in a decorative ring divided into one arc
segment per group (as in a Voronoi-treemap infographic).  Each segment
spans the angular extent actually occupied by its group's cells, so the
ring lines up with a hierarchical layout produced by
\code{voronoi_map(..., group =)}.
}
\examples{
vm <- voronoi_map(c(5, 3, 8, 4, 6, 2),
                  group = c("A", "A", "B", "B", "C", "C"),
                  clip = clip_circle(), seed = 1)
ggvmap(vm, palette = "alger") |> vm_add_ring(palette = "alger")
ggvmap(vm, palette = "alger") |>
  vm_add_ring(style = "arc", palette = "alger", values = TRUE,
              values_sep = " - ")   # ASCII sep keeps pdf() happy everywhere
}
